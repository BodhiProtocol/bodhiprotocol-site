# Copy this file to scripts/cross-post/.env.local (or export these in your shell)
# and fill in your own values. Never commit real secrets.
#
# Run with: node --env-file=scripts/cross-post/.env.local scripts/cross-post/publish-essay.mjs <slug>

# Public URL used for canonical links and as the default cover image source.
CROSSPOST_SITE_URL=https://bodhiprotocol.com

# --- Medium (optional) ---
# Medium stopped issuing new integration tokens in 2025. Check Settings ->
# Security and apps -> "Integration tokens" on medium.com -- if there's
# nothing there, your account can't get one, and the script will write a
# paste-ready file instead (which works fine without any of this).
MEDIUM_INTEGRATION_TOKEN=
# Optional -- the script fetches this from /v1/me if left blank.
MEDIUM_USER_ID=

# Substack has no public API and needs no credentials here -- the script
# always writes a paste-ready HTML file for it.
