# Public site URL used for canonical links, Open Graph images, and JSON-LD.
# Optional: falls back to the production domain if unset. Vercel preview
# deployments automatically use their own VERCEL_URL instead.
NEXT_PUBLIC_SITE_URL=https://bodhiprotocol.com
